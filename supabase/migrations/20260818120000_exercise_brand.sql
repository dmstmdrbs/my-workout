-- 종목에 기구 제조사를 더한다.
--
-- 같은 "체스트 프레스"라도 제조사가 다르면 같은 중량이 같은 무게가 아니다.
-- 브랜드가 다르면 별개의 종목 행으로 두고, 1RM·중량 추이도 섞이지 않게 한다.
--
-- `equipment`가 그렇듯 값 검증은 앱이 한다(`ExerciseBrand`). DB 제약으로
-- 굳히면 브랜드 목록을 늘릴 때마다 마이그레이션이 필요해진다.
--
-- 비파괴적이다. 기존 행은 null이 되고, 이 컬럼을 읽지 않는 지금 프론트엔드는
-- 그대로 동작하므로 DB를 먼저 적용해도 안전하다.
alter table public.exercises add column if not exists brand text;
