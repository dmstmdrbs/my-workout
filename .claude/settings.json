{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "GUARD=\"$(git rev-parse --show-toplevel 2>/dev/null)/scripts/agent-guard.mjs\"; if [ -f \"$GUARD\" ]; then node \"$GUARD\"; else exit 0; fi",
            "timeout": 10,
            "statusMessage": "워크플로 규칙 확인"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$(git rev-parse --show-toplevel 2>/dev/null)/scripts/session-git-status.sh\"; if [ -f \"$S\" ]; then bash \"$S\"; else exit 0; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
